package grammarPractice5

func check(err error) {
	if err != nil {
		panic(err)
	}
}

func ControllErrorTest() {
	/* c, err := Conn()
	check(err)

	f, err := os.Open(FILE_PATH)
	check(err)

	에러 확인 함수를 사용하기 위해 만들어본 예시일 뿐
	실제로 실행되지는 않음.
	*/
}
